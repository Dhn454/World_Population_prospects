---
apiVersion: v1
kind: Service
metadata:
  name: test-redis-service
  labels:
    app: test-redis-service
spec:
  selector:
    app: test-redis-deployment
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
